---
version: "v0.2.0"
date: "2026-07-15"
channel: product
---

hub 0.2.0 — user-scoped live federation

### Breaking

- consent flip to org-free, user-scoped OAuth grants (Stage B, Phase 3)

### Features

- H3 mints the tool client an offline grant, returned as hubRefreshToken (Stage B, Phase 4)
- optional per-call workspace argument on tools (Stage B, Phase 3)
- org default in the CLI; default marker + set-as-default + key pin in the dashboard
- user-scoped key surfaces + default organization
- ONE shared workspace-selection helper; selector threaded into every credential path
- migration 0017 — user-scoped credentials (api_keys optional pin + user_settings)

### Fixes

- close three user-scoped-credential corners (Stage A double-check)
- orgs schema gains status + isDefault (belongs with the API-surfaces commit)
